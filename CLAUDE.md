# منصّة بَهْجَة — قواعد العمل على المشروع

مرجع دائم لأي جلسة عمل قادمة على هذا المستودع.

## هوية الكوميتات (مهم — يُضبط في بداية كل جلسة)

صاحب المشروع هو المؤلف، وClaude مؤلّف مشارك:

```bash
git config user.name "Mahmoud Abu Aisha"
git config user.email "63356182+mahmoudabuaisha@users.noreply.github.com"
```

وكل رسالة كوميت تنتهي بسطر المؤلف المشارك:

```
Co-Authored-By: Claude <noreply@anthropic.com>
```

## دورة الاعتماد (لا تُخالَف)

1. التعديل يُنفَّذ على فرع `change/<وصف-مختصر>` مقطوع من **آخر** `deploy/hostinger`
   (القطع من رأس فرع النشر يمنع تعارضات `public/build`).
2. تُرسَل صور PNG (أو GIF للحركة) للمعاينة — **لا نشر قبل كلمة «اعتمد»**.
3. بعد «اعتمد»: يُفتح Pull Request إلى `deploy/hostinger`، ويدمجه صاحب المشروع بنفسه.
4. الدمج يُطلق `.github/workflows/deploy.yml` فينشر على Hostinger تلقائياً
   (`bash ~/update-hostinger.sh`) — بلا أي أمر في التيرمنال.

`deploy/hostinger` هو الفرع الافتراضي للمستودع، فتُحتسب كل دمجة مساهمةً في حساب
صاحب المشروع على GitHub.

## تحذير: حذف ملفات الأصناف يوقع الموقع

حدث فعلاً (11 سبتمبر 2026): حذف مزوّد لوحة Filament للفرق أوقع الموقع بـ 500
على كل صفحة. السبب أن خريطة أصناف composer على الاستضافة بقيت تشير إلى ملف
مفقود، فعجز artisan عن الإقلاع — ففشلت `optimize:clear` **قبل** أن تمسح
الذاكرة القديمة، وبقيت الاستضافة عالقة.

لذلك: **لا تُحذف ملفات أصناف وتُغيَّر وظائف في تعديل واحد.** المرحلتان:

1. تعديل أول: إلغاء تسجيل المزوّد/المورد وتحويل المسارات — **بلا حذف ملف**،
   فتبقى الخريطة سليمة وينشر بسلام.
2. تعديل ثانٍ منفصل بعد التأكد: حذف الملفات المعطَّلة.

## البناء قبل كل PR

أصول Vite مُتتبَّعة على فرع النشر (الاستضافة المشتركة لا تبني)، فيلزم بناؤها:

```bash
# احذف كتلة bunny من vite.config.js مؤقتاً (لا شبكة للخطوط في بيئة العمل)
npm run build
git checkout vite.config.js
```

ثم `git add public/build` ضمن الكوميت.

## قبل الإرسال

```bash
php artisan test --compact    # يجب أن تمر كل الاختبارات
```

## ثوابت التصميم

- الهوية زرقاء-وردية: `--color-brand-500: #3b93e4`، الوردي `#ec4899`/`#db2777`.
  **لا بنفسجي** — التدرّجات بفواصل حادة كي لا يتولّد من مزج الأزرق بالوردي.
- الواجهة عربية RTL بالكامل، والنصوص بصيغة المخاطَب الجمعي («شاركوا»، «سجّلوا»).
- `.input` بحجم خط 16px (أقل منه يُجبر سفاري iOS على تقريب الصفحة عند اللمس).
- كل حركة تُعطَّل تلقائياً مع `prefers-reduced-motion`.
- الصور تمر بـ `ImageService` (تصغير + مسح EXIF/GPS + نسخة بطاقة webp).

## PWA — التثبيت على الأجهزة

- المانيفست والـ Service Worker يُقدَّمان عبر Blade (`resources/views/pwa/`) كي
  يُحقن رقم الإصدار وقائمة أصول Vite وقت الطلب. إصدار الـ SW = بصمة
  `public/build/manifest.json`، فكل بناء جديد يُحدّث الكاش تلقائياً.
- الأيقونات مولَّدة من `public/brand/logo.png`: الطلسم (الوجه بالتاج) على
  تدرّج الهوية. أيقونة iOS **معتمة إجباراً** — سفاري يرسم الشفافية سوداء.
- لقطات المانيفست (`public/icons/screenshot-*.png`) تُلتقط من الموقع نفسه؛
  أعيدوا التقاطها إن تغيّر تصميم الرئيسية أو صفحة الفعاليات.
- **لا `skipWaiting()` عند التثبيت**: النسخة الجديدة تنتظر ضغطة المستخدم على
  شريط «حدّثوا الآن». وإعادة التحميل عند `controllerchange` مشروطة بأن يكون
  المستخدم هو من طلبها — وإلا سُحبت الصفحة من كل زائر جديد عند أول تثبيت.
- `PRIVATE_PATHS` في الـ SW: صفحات الحساب لا تُخزَّن أبداً. الهاتف في غزة
  يتشاركه أكثر من فرد.

## إشعارات الدفع (Web Push)

- التعمية وتوقيع VAPID **مكتوبان في المشروع** (`app/Services/Push/`) بلا أي
  حزمة خارجية: الاستضافة بلا gmp ولا bcmath، وإضافة حزمة تعني أن نشراً لا
  يشغّل `composer install` يُسقط الموقع. لا تستبدلوهما بمكتبة دون التحقّق.
- `WebPushTest` يقيس التعمية بمتّجه **RFC 8291** الرسمي. إن كسر تعديلٌ هذا
  الاختبار فالرسائل سترفضها كل خدمات الدفع بصمت — لا تُخفَّف التوقّعات.
- المفاتيح تُولَّد مرّة بـ `php artisan push:keys` وتُوضع في `.env` على
  الخادم. تغييرها يُبطل كل اشتراكات الأجهزة القائمة.
- `UserNotification::PUSHABLE` يحدّد ما يوقظ الهاتف، و`quiet_from/until`
  تمنع الإرسال ليلاً إلا للإلغاء والتغيير.
- «غداً في حيّكم» أمرٌ مجدول (`push:tomorrow`) الساعة 18:30، محميّ من
  التكرار بمفتاح كاش لكل عائلة/يوم.
