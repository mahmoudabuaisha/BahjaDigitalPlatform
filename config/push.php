<?php

return [

    /*
    |--------------------------------------------------------------------------
    | مفاتيح VAPID
    |--------------------------------------------------------------------------
    |
    | تُولَّد مرة واحدة بالأمر `php artisan push:keys` وتُوضع في .env.
    | المفتاح العام يصل المتصفّح، والخاص لا يغادر الخادم أبداً. تغييرهما
    | يُبطل كل الاشتراكات القائمة، فلا يُغيَّران إلا لضرورة.
    |
    */

    'public_key' => env('VAPID_PUBLIC_KEY'),

    'private_key' => env('VAPID_PRIVATE_KEY'),

    // من نحن في نظر خدمة الدفع — بريد يُراسَل عند مشكلة
    'subject' => env('VAPID_SUBJECT', 'mailto:info@bahjagaza.com'),

    // كم تحتفظ خدمة الدفع بالإشعار إن كان الهاتف مطفأً (بالثواني)
    'ttl' => (int) env('PUSH_TTL', 86400),

    // ساعات الهدوء: لا يُوقَظ بيت فيه أطفال نيام
    'quiet_from' => (int) env('PUSH_QUIET_FROM', 22),

    'quiet_until' => (int) env('PUSH_QUIET_UNTIL', 7),

];
